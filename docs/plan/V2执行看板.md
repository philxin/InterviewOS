# InterviewOS V2 执行看板

## 1. 文档目标

用于记录 V2 当前阶段的执行状态、交付物与阻塞项，按任务流推进，不按天拆分。

V2 范围：

- 用户系统与数据隔离
- 训练方向初始化（轻量岗位选择）
- 批量知识导入与简单标签体系
- 更真实的题目结构
- 更可信的评分反馈结构
- 前后端 V2 主链路联调

V2 明确不做：

- 简历深度解析
- 复杂岗位画像
- 多轮对话
- 复杂图表统计
- 社交 / 排行榜
- 过多文件格式导入
- 超复杂标签系统
- 双数据库拆分
- 复杂权限体系
- 语义检索 / RAG

---

## 2. 看板规则

状态定义：

- `TODO`：未开始
- `DOING`：进行中（最多 1-2 个并行卡片）
- `BLOCKED`：被外部依赖阻塞
- `DONE`：完成并通过当前验收标准

优先级定义：

- `P0`：阻塞 V2 主路径
- `P1`：重要但不阻塞 V2 主路径
- `P2`：增强项 / 预留项

完成标准（DoD）：

- 代码已落库或文档已冻结
- 主路径自测通过
- 错误路径有可预期返回
- 接口 / 数据 / 页面变更已同步文档

---

## 3. 当前基线（2026-03-16）

- 后端：`V2-01/V2-02` 已完成，已具备 `Auth + Security + AppUser + onboarding` 骨架；知识点 owner 化、批量导入、训练新模型尚未落地
- 前端：代码仍以 V1 页面流和 V1 接口契约为主，尚未切换真实登录链路与路由守卫
- 文档：V2 规划包已冻结，进入按看板推进代码实现阶段

结论：当前处于 **V2 P0 主链路开发阶段（后端先行）**。下一步应优先推进 `V2-03`，并在 `V2-03` 稳定后启动 `V2-04` 与前端 `V2-07` 并行改造。

---

## 4. 执行看板

### TODO

| ID | 优先级 | 任务 | 交付物 | 验收标准 |
|---|---|---|---|---|
| V2-04 | P0 | 批量导入接口 | `/knowledge/batch-import` | 可一次导入多条知识点，并返回失败项 |
| V2-05 | P0 | 训练主模型切换 | `TrainingSession`、`TrainingQuestion`、新训练接口 | 可开始训练会话、提交回答、查询历史与详情，并支持 session 摘要 |
| V2-06 | P0 | 反馈结构重构 | 新反馈 DTO / Prompt / 结果字段 | 返回 `score/band/majorIssue/missingPoints/betterAnswerApproach/naturalExampleAnswer`，且 `band` 由服务端确定性映射 |
| V2-07 | P0 | 前端登录主链路 | 登录页、auth store、路由守卫 | 未登录跳转登录页，登录成功可持久化 token |
| V2-08 | P0 | 前端知识点与导入改造 | Dashboard、KnowledgeForm、KnowledgeImport | 知识点支持标签与批量导入 |
| V2-09 | P0 | 前端训练页与结果页改造 | `TrainingSessionView`、`ResultView` | 主链路从开始训练到结果页一次走通 |
| V2-10 | P0 | V2 前后端联调与回归 | 联调记录、自测结果 | 用户隔离、批量导入、训练会话、结果页结构全部通过 |
| V2-11 | P1 | 提示与训练引导 | hint 接口 + 训练页提示交互 | 提示只给思路，不直接给完整答案 |
| V2-12 | P1 | 首页概览与最小进步感 | dashboard overview + 首页摘要模块 | 首页可展示薄弱项摘要与最近训练摘要 |
| V2-13 | P1 | 文件导入任务化 | `knowledge_file_import` + 状态接口 + 前端页面 | 文件导入可异步查看状态 |
| V2-14 | P1 | 性能与页面体验优化 | LLM 耗时优化、前端主路径样式优化 | 训练等待与页面完成度较 V1 有明显改善 |
| V2-15 | P2 | 今日推荐训练包 | 推荐接口 + 首页入口 | 系统可给出 3-5 题训练包 |
| V2-16 | P2 | 自动组题 | 多题 session | 支持按标签/薄弱项/历史表现组题 |
| V2-17 | P2 | 回练提醒机制 | 权重更新策略 + 回练入口 | 可对薄弱项产生后续训练驱动 |

### DOING

| ID | 优先级 | 任务 | 当前进展 | 下一步 |
|---|---|---|---|---|
| V2-05 | P0 | 训练主模型切换 | `V2-04` 已完成，知识点批量导入与失败项回传已落地 | 设计 `TrainingSession/TrainingQuestion` 实体与新训练接口迁移路径 |

### BLOCKED

| ID | 阻塞项 | 影响范围 | 解除条件 |
|---|---|---|---|
| BLK-02 | V1 旧表到 V2 新训练模型的迁移策略尚未执行 | `V2-05` 训练主模型切换 | 明确开发环境是否允许直接重建数据，或定义旧数据归属方案 |

### DONE

| ID | 完成项 | 结果 |
|---|---|---|
| DOC-01 | V2 版本规划冻结 | 已新增 `V2版本规划.md`，明确 P0/P1/P2 与不做项 |
| DOC-02 | V2 API 契约冻结 | 已以 `docs/plan/API接口设计.md` 替换 V1 契约，完成认证、知识点、训练、Dashboard 契约定义 |
| DOC-03 | V2 数据库设计冻结 | 已新增 `V2数据库设计.md`，明确 `app_user / knowledge / knowledge_tag / training_session / training_question / knowledge_file_import` |
| DOC-04 | V2 后端实现计划冻结 | 已新增 `V2后端实现计划.md`，明确模块拆分、实现顺序与风险 |
| DOC-05 | V2 前端实现计划冻结 | 已新增 `V2前端实现计划.md`，明确登录、路由守卫、训练页、结果页与导入页改造 |
| DOC-06 | V2 开发任务清单冻结 | 已新增 `V2开发任务清单.md`，完成 P0/P1/P2 任务拆分 |
| DOC-07 | `docs/plan` V1 残留清理 | 已删除 `docs/plan` 下 V1 文档，仅保留当前 V2 文档包 |
| V2-01 | 后端用户与鉴权骨架 | 已落地 `AppUser`、JWT、`/auth/register`、`/auth/login`、`/auth/me`，并将受保护接口切换为未登录返回 `401` |
| V2-02 | 训练方向初始化接口 | 已落地 `PATCH /users/me/onboarding`，登录用户可保存 `targetRole`，并完成控制器/服务测试覆盖 |
| V2-03 | 知识点模型升级 | 已落地 `Knowledge/KnowledgeTag` owner 模型、标签规范化、`GET /knowledge/tags` 与归档语义，并通过后端测试 `50/50` |
| V2-04 | 批量导入接口 | 已落地 `POST /knowledge/batch-import`、逐项校验、失败项回传与 `422` 语义，并通过后端测试 `55/55` |
| V2-03 | 知识点模型升级 | 已落地 `Knowledge/KnowledgeTag` owner 模型、标签规范化、`GET /knowledge/tags` 与归档语义，并通过后端测试 `50/50` |

---

## 5. 任务依赖关系（执行顺序）

P0 主链路：

`V2-01 -> V2-02 -> V2-03 -> V2-04 -> (V2-05 + V2-06) -> V2-07 -> V2-08 -> V2-09 -> V2-10`

并行支线：

- `V2-07` 可在 `V2-01` 基本稳定后并行
- `V2-11`、`V2-12`、`V2-13`、`V2-14` 在 `V2-10` 前不应抢占主线
- `V2-15`、`V2-16`、`V2-17` 仅在 P0 稳定后进入排期

---

## 6. 风险与应对

| 风险 | 影响 | 应对措施 |
|---|---|---|
| 范围膨胀，把 P2 提前做到 P0 | 主路径延期 | 严格以 `V2-01 ~ V2-10` 作为首批上线范围 |
| 评分结构改版后仍然“不可信” | 结果页价值下降 | 先冻结反馈结构，再单独验收 Prompt 输出 |
| 训练模型新旧并存导致代码混乱 | 维护成本高 | 明确 V2 主链路只走 `TrainingSession/TrainingQuestion` |
| Security 改造后前端大面积 401 | 联调效率下降 | 先完成 `/auth/me` 和登录守卫，再逐步切受保护接口 |
| 批量导入引入脏数据 | 知识点质量下降 | 做逐项校验与失败项回传，禁止静默跳过 |

---

## 7. 更新记录

| 日期 | 变更内容 | 维护人 |
|---|---|---|
| 2026-03-15 | 初始化 V2 执行看板，明确当前处于文档冻结完成、代码未启动阶段 | Codex |
| 2026-03-15 | 完成 DOC-01 ~ DOC-07：V2 文档包收口，并清理 `docs/plan` V1 残留 | Codex |
| 2026-03-15 | 完成 V2-01：落地后端认证骨架、JWT 配置与测试环境，后端测试 `38/38` 通过 | Codex |
| 2026-03-15 | 完成 V2-02：落地 `PATCH /users/me/onboarding`，后端测试 `43/43` 通过 | Codex |
| 2026-03-16 | 同步看板基线到代码现状：将 `V2-03` 设为 DOING，并明确后续并行顺序（`V2-04` + `V2-07`） | Codex |
| 2026-03-16 | 完成 `V2-03`：知识点 owner 隔离、标签模型、归档语义与标签查询接口落地，后端测试 `50/50` 通过 | Codex |
| 2026-03-16 | 完成 `V2-04`：批量导入 DTO、`KnowledgeImportService`、失败项回传与 `422` 语义落地，后端测试 `55/55` 通过 | Codex |
