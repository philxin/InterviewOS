# InterviewOS V3 执行看板

## 1. 文档目标

用于记录 V3 当前阶段的执行状态、交付物与阻塞项，按任务流推进，不按天拆分。

V3 范围：

- 文件导入升级为 document / chunk / embedding pipeline
- 文件级知识库与最小可用检索能力
- 训练题目、提示、反馈接入 retrieval context
- 训练引用快照与结果可追溯性
- 自动知识点识别的结构预留

V3 明确不做：

- 通用文档问答平台
- 跨用户共享知识库
- 复杂 OCR
- 多 Provider 编排
- 知识图谱
- 首版复杂 rerank 链路
- 不经用户确认直接接管 `Knowledge`

---

## 2. 看板规则

状态定义：

- `TODO`：未开始
- `DOING`：进行中（最多 1-2 个并行卡片）
- `BLOCKED`：被外部依赖阻塞
- `DONE`：完成并通过当前验收标准

优先级定义：

- `P0`：阻塞 V3 主路径
- `P1`：重要但不阻塞 V3 主路径
- `P2`：增强项 / 预留项

完成标准（DoD）：

- 代码已落库或文档已冻结
- 主路径自测通过
- 错误路径有可预期返回
- 接口 / 数据 / 页面变更已同步文档
- 涉及检索的能力有用户隔离与最小可观测性

---

## 3. 当前基线（2026-03-24）

- 文档：V2 规划包已归档到 `docs/archive/v2/`，V3 规划包已在 `docs/plan/` 冻结完成
- 后端：已具备 `KnowledgeFileImport` 异步导入骨架、`TrainingSession/TrainingQuestion` 主模型、`Spring AI` 与 `pgvector` 依赖基础
- 前端：已具备文件导入页、训练页、结果页、历史页、首页概览等 V2 主链路页面
- 当前缺口：`KnowledgeDocument`、`KnowledgeChunk`、`TrainingQuestionReference`、`EmbeddingService`、`RetrievalService`、训练引用快照、V3 检索安全与 trace

结论：当前处于 **V3 文档冻结完成、代码未正式启动阶段**。V2 已归档，下一步应进入 `V3-01 ~ V3-03` 的后端骨架搭建。

---

## 4. 执行看板

### TODO

| ID | 优先级 | 任务 | 交付物 | 验收标准 |
|---|---|---|---|---|
| V3-01 | P0 | 冻结状态枚举、chunk 切片策略、embedding 配置与低命中降级规则 | 配置项清单 + 设计结论 | 文档冻结，切片 / 模型 / fallback 规则明确可实现 |
| V3-02 | P0 | 新增 `KnowledgeDocument / KnowledgeChunk / TrainingQuestionReference` 数据模型 | 实体、Repository、迁移脚本 | 表结构、索引、唯一约束与 user 隔离字段落地 |
| V3-03 | P0 | 升级文件导入 pipeline | `KnowledgeFileImportProcessor` 改造 | 支持 `PROCESSING -> CHUNKING -> EMBEDDING -> SUCCESS/PARTIAL/FAILED` |
| V3-04 | P0 | 实现 `EmbeddingService / RetrievalService` | 服务实现 + 查询接口 | 可在当前用户范围内返回 topK 引用片段 |
| V3-05 | P0 | 训练链路接入 retrieval context 与引用快照 | 训练服务改造 + DTO 扩展 | 出题 / 提示 / 反馈至少一条主链路可返回 `references` |
| V3-06 | P1 | 前端接入文档、检索验证与训练引用展示 | 页面与 API 接入 | 文件导入状态、文档列表、结果页引用展示可用 |
| V3-07 | P0 | 安全与回归验证 | 测试用例 + 回归记录 | 双用户绝不交叉召回，低命中降级、幂等重试通过 |
| V3-08 | P1 | 自动知识点识别候选结构预埋 | `KnowledgeConcept` 设计 / 首版实现 | 候选知识点可落库，不影响现有 `Knowledge` 主链路 |

### DOING

| ID | 优先级 | 任务 | 当前进展 | 下一步 |
|---|---|---|---|---|
| - | - | - | - | 当前无 DOING 卡片，等待 `V3-01` 启动 |

### BLOCKED

| ID | 阻塞项 | 影响范围 | 解除条件 |
|---|---|---|---|
| - | - | - | 当前无 BLOCKED 项 |

### DONE

| ID | 完成项 | 结果 |
|---|---|---|
| DOC-01 | V2 文档归档 | 已将 V2 文档迁移到 `docs/archive/v2/plan/`，并补齐归档索引与交接摘要 |
| DOC-02 | V3 版本规划冻结 | 已新增 `V3版本规划.md`，明确 P0/P1/P2、系统边界与不做项 |
| DOC-03 | V3 数据库设计冻结 | 已新增 `V3数据库设计.md`，明确 `knowledge_document / knowledge_chunk / training_question_reference` 等结构 |
| DOC-04 | V3 API 契约冻结 | 已新增 `V3接口设计.md`，明确文件导入、检索与训练增强接口方向 |
| DOC-05 | V3 开发任务清单冻结 | 已新增 `V3开发任务清单.md`，完成 P0/P1/P2 任务拆分 |
| DOC-06 | V3 执行看板初始化 | 已建立 V3 看板，明确当前基线、任务顺序与风险项 |

---

## 5. 任务依赖关系（执行顺序）

P0 主链路：

`V3-01 -> V3-02 -> V3-03 -> V3-04 -> V3-05 -> V3-07`

并行支线：

- `V3-06` 可在 `V3-04` 接口基本稳定后并行推进
- `V3-08` 仅在 P0 检索主链路稳定后进入排期

---

## 6. 风险与应对

| 风险 | 影响 | 应对措施 |
|---|---|---|
| 把“自动知识点识别”提前做到 P0 | 主路径延期，复杂度失控 | 严格以 document / chunk / retrieval 为 P0 主线 |
| 检索未按 `user_id` 过滤 | 严重数据泄露 | Repository / SQL 层强制 owner 过滤，并做双用户回归测试 |
| embedding 模型与维度过早固化错误 | 后续重建成本高 | 在 `V3-01` 冻结模型、维度、迁移策略 |
| 导入任务缺少幂等与部分失败状态 | 向量重复、用户状态误判 | 增加唯一约束、`PARTIAL` 状态与失败重试策略 |
| 评分引入动态检索后不稳定 | 用户不信任结果 | 训练时固定引用集合并持久化快照 |
| 检索结果被 prompt injection 污染 | 模型输出偏移或违规 | 引用文本进入 prompt 时明确降权为“材料”，并做基础过滤 |

---

## 7. 更新记录

| 日期 | 变更内容 | 维护人 |
|---|---|---|
| 2026-03-24 | 初始化 V3 执行看板，确认当前处于文档冻结完成、代码未启动阶段 | Codex |
| 2026-03-24 | 完成 DOC-01：V2 规划文档归档到 `docs/archive/v2/`，并补齐 V2 归档索引与交接摘要 | Codex |
| 2026-03-24 | 完成 DOC-02 ~ DOC-05：V3 版本规划、数据库设计、接口设计、开发任务清单冻结 | Codex |

