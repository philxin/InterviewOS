# InterviewOS V2 联调与回归记录

## 1. 记录目标

用于沉淀 `V2-10` 前后端联调与回归结论，明确当前主链路的自动化验证范围、验证结果与残余风险。

---

## 2. 验证时间

- 日期：2026-03-17
- 执行人：Codex

---

## 3. 执行命令

后端：

```bash
cd backend && ./mvnw test
```

前端：

```bash
cd frontend && npm run build
```

执行结果：

- 后端测试：`66/66` 通过
- 前端构建：`vue-tsc -b && vite build` 通过

---

## 4. 联调结论

### 4.1 认证与安全边界

- `register / login / me` 契约已通过控制器与前端主链路对齐验证
- 未登录访问受保护资源返回 `401`
- CORS 白名单与安全响应头正常
- Bearer Token 失效时，前端会清理本地登录态并跳转 `/login`

对应依据：

- `backend/src/test/java/com/philxin/interviewos/controller/AuthControllerTest.java`
- `backend/src/test/java/com/philxin/interviewos/config/SecurityConfigTest.java`
- `frontend/src/api/client.ts`
- `frontend/src/router/index.ts`

### 4.2 用户隔离

- 知识点查询、更新、删除均按 owner 范围访问
- 训练会话详情查询按 owner 范围访问，非本人记录返回 `404`

对应依据：

- `backend/src/test/java/com/philxin/interviewos/service/KnowledgeServiceTest.java`
- `backend/src/test/java/com/philxin/interviewos/service/TrainingSessionServiceTest.java`

### 4.3 批量导入

- `POST /knowledge/batch-import` 支持部分成功 `201`
- 全部失败返回 `422`
- 前端已保留并展示 `failedItems`

对应依据：

- `backend/src/test/java/com/philxin/interviewos/controller/KnowledgeControllerTest.java`
- `frontend/src/views/KnowledgeImportView.vue`
- `frontend/src/api/client.ts`

### 4.4 训练会话、结果页与历史页

- 前端训练入口已切到 `POST /training/sessions`
- 回答提交已切到 `POST /training/sessions/{sessionId}/answers`
- 结果页已消费 `score / band / majorIssue / missingPoints / betterAnswerApproach / naturalExampleAnswer / weakTags / masteryBefore / masteryAfter`
- 历史页已消费 `GET /training/sessions` 分页摘要结构
- 训练详情页刷新后可通过 `GET /training/sessions/{sessionId}` 恢复会话或结果

对应依据：

- `backend/src/test/java/com/philxin/interviewos/controller/TrainingSessionControllerTest.java`
- `frontend/src/views/TrainingView.vue`
- `frontend/src/views/TrainingSessionView.vue`
- `frontend/src/views/ResultView.vue`
- `frontend/src/views/HistoryView.vue`

---

## 5. 当前验收判断

`V2-10` 按当前 P0 DoD 可判定完成：

- 主路径代码已落库
- 自动化测试与生产构建通过
- 鉴权、owner 隔离、批量导入、训练会话、结果页字段映射均有验证依据
- 文档、看板与版本规划已同步

---

## 6. 残余风险

- 尚未做真实浏览器手工走查，当前结论以自动化测试和构建验证为主
- `LLMServiceImpl` 的真实外部模型调用未在本次联调中使用线上 OpenAI 环境验证
- 旧接口 `/api/training/start|submit|history*` 仍在代码中保留兼容实现，后续需安排下线窗口避免双模型长期并存

---

## 7. 下一步

- 按 `V2迁移策略冻结.md` 执行数据库迁移与回填（按环境策略）
- 完成线上环境的手工冒烟回归并补充发布结论
