# InterviewOS V2 开发任务清单

## 📋 任务概览

本清单基于以下文档拆解：

- [V2版本规划.md](./V2版本规划.md)
- [API接口设计.md](./API接口设计.md)
- [V2数据库设计.md](./V2数据库设计.md)
- [V2迁移策略冻结.md](./V2迁移策略冻结.md)
- [V2后端实现计划.md](./V2后端实现计划.md)
- [V2前端实现计划.md](./V2前端实现计划.md)

任务拆分原则：

1. 先完成 P0 主链路
2. P1 不阻塞 P0 上线
3. P2 只预留，不混入首批开发

---

## 📍 当前进度（2026-03-18）

- 已完成：文档冻结（DOC-01 ~ DOC-07）、后端用户与鉴权骨架（V2-01）、训练方向初始化（V2-02）
- 已完成：V2-03（知识点 owner 化 + 标签模型）、V2-04（批量导入接口）、V2-05（训练主模型切换）、V2-06（反馈结构重构）、V2-07（前端登录主链路）、V2-08（前端知识点与导入改造）、V2-09（前端训练主链路改造）、V2-10（联调与回归）、V2-11（提示与训练引导）、V2-12（首页概览与最小进步感）、V2-13（文件导入任务化）、V2-14（性能与页面体验优化）、V2-15（今日推荐训练包）、V2-16（自动组题）、V2-17（回练提醒机制）、空态/错误态统一组件化
- 已完成：V1 `training_record` 到 V2 新训练模型迁移策略冻结（见 `V2迁移策略冻结.md`）

---

## 第一阶段：文档冻结与技术准备

### Day 1

- [x] 确认 V2 版本边界与不做项
- [x] 确认 V2 API 契约
- [x] 确认 V2 数据库设计
- [x] 确认 V2 前后端实现计划

### Day 2

- [x] 后端补充 JWT 依赖方案
- [x] 确认 `TargetRole` 枚举清单与密码策略
- [x] 确认数据库迁移策略（开发 / 测试环境）
- [x] 确认 V1 旧表 `training_record` 的处理策略
- [x] 冻结 P0 DTO 命名和字段

---

## 第二阶段：后端 P0 骨架

### 模块 A：用户与鉴权

- [x] 新增 `AppUser` 实体
- [x] 新增 `AppUserRepository`
- [x] 新增 `AuthService`
- [x] 新增 `JwtTokenService`
- [x] 新增 `JwtAuthenticationFilter`
- [x] 改造 `SecurityConfig`
- [x] 新增 `AuthController`
- [x] 新增 `/auth/register`
- [x] 新增 `/auth/login`
- [x] 新增 `/auth/me`
- [x] 增加密码哈希与校验逻辑

### 模块 B：用户训练方向

- [x] 在 `AppUser` 中增加 `targetRole`
- [x] 新增 `UserService`（承载 onboarding 逻辑）
- [x] 新增 `UserController`
- [x] 新增 `PATCH /users/me/onboarding`

### 模块 C：知识点模型升级

- [x] 改造 `Knowledge` 实体，增加 `user / sourceType / updatedAt`
- [x] 新增 `KnowledgeTag` 实体
- [x] 改造 `KnowledgeRepository`
- [x] 新增 `KnowledgeTagRepository`
- [x] 改造 `KnowledgeService` 为 owner 模型
- [x] 改造 `KnowledgeResponse`
- [x] 改造创建 / 更新 DTO，支持 `tags`
- [x] 明确知识点归档语义，保留历史训练数据

### 模块 D：批量导入

- [x] 新增 `BatchImportKnowledgeRequest`
- [x] 新增 `BatchImportKnowledgeResponse`
- [x] 新增 `KnowledgeImportService`
- [x] 新增 `POST /knowledge/batch-import`
- [x] 支持逐项校验与失败项返回

### 模块 E：训练主模型切换

- [x] 新增 `TrainingSession` 实体
- [x] 新增 `TrainingQuestion` 实体
- [x] 新增 `TrainingSessionRepository`
- [x] 新增 `TrainingQuestionRepository`
- [x] 新增 `TrainingSessionService`
- [x] 新增 `TrainingSessionController`
- [x] 新增 `POST /training/sessions`
- [x] 新增 `POST /training/sessions/{sessionId}/answers`
- [x] 新增 `GET /training/sessions`
- [x] 新增 `GET /training/sessions/{sessionId}`

### 模块 F：反馈结构重构

- [x] 新增 `FeedbackGenerationRequest`
- [x] 新增 `FeedbackGenerationResult`
- [x] 重构 `PromptTemplate` 评分模板
- [x] 重构 `LLMService` 反馈接口
- [x] 新增 `score / band / majorIssue / missingPoints / betterAnswerApproach / naturalExampleAnswer`
- [x] 固化 `score -> band` 服务端映射规则
- [x] 保留掌握度更新逻辑并迁移到新模型
- [x] 新增 `MasteryService`
- [x] 增加 session 级摘要字段与聚合规则

---

## 第三阶段：前端 P0 主链路

### 模块 A：认证与登录态

- [x] 改造 `types/index.ts` 增加 V2 用户类型
- [x] 改造 `api/client.ts` 的 401 处理
- [x] 新增 `authAPI`
- [x] 新增 `auth` store
- [x] 删除或迁移旧 `user.ts`，避免双 store 并存
- [x] 新增 `LoginView.vue`
- [x] 增加登录后 token 持久化
- [x] 增加登出清理逻辑

### 模块 B：路由与守卫

- [x] 改造 `router/index.ts`
- [x] 新增登录守卫
- [x] 新增 onboarding 守卫
- [x] 新增 `/login`
- [x] 新增 `/onboarding`
- [x] 调整训练页与结果页路由为 session 结构

### 模块 C：知识点与导入

- [x] 改造 `Knowledge` 类型，增加 `tags / sourceType / updatedAt`
- [x] 改造 `knowledgeAPI`
- [x] 改造 `DashboardView.vue`
- [x] 改造 `KnowledgeFormView.vue` 支持标签
- [x] 新增 `KnowledgeImportView.vue`
- [x] 接入批量导入接口

### 模块 D：训练主链路

- [x] 改造 `training` store
- [x] 改造 `trainingAPI`
- [x] 新增 `TrainingSessionView.vue`
- [x] 调整训练启动逻辑为 `POST /training/sessions`
- [x] 调整提交逻辑为 `POST /training/sessions/{sessionId}/answers`
- [x] 支持展示题型与难度

### 模块 E：结果页重构

- [x] 重构 `ResultView.vue`
- [x] 新增 `TrainingFeedbackBand.vue`
- [x] 展示档位与主要问题
- [x] 展示缺失点
- [x] 展示更好回答思路
- [x] 展示自然参考表达
- [x] 展示掌握度前后变化

### 模块 F：历史页重构

- [x] 改造 `HistoryView.vue`
- [x] 接入新的训练历史摘要结构
- [x] 支持按知识点筛选
- [x] 支持跳转单次训练详情
- [x] 支持分页

---

## 第四阶段：P0 联调与回归

### 后端联调

- [x] 注册 / 登录 / me 联调通过
- [x] token 鉴权联调通过
- [x] 用户 A / 用户 B 数据隔离验证通过
- [x] 批量导入联调通过
- [x] 新训练会话接口联调通过
- [x] 新结果页字段联调通过

### 前端回归

- [x] 登录态刷新后仍然有效
- [x] 未登录访问受保护页会正确跳转
- [x] 知识点 CRUD 未被破坏
- [x] 批量导入失败项可见
- [x] 训练页和结果页无字段映射错误
- [x] 历史页读取正常

### 技术回归

- [x] 后端单元测试通过
- [x] 后端集成测试通过
- [x] 前端 `vue-tsc` 通过
- [x] 前端构建通过

---

## 第五阶段：P1 增强

### 后端

- [x] 新增 `KnowledgeFileImport` 实体与任务状态表
- [x] 新增文件导入接口
- [x] 新增 hint 接口
- [x] 新增 dashboard overview 接口
- [x] 优化 LLM 调用耗时统计

### 前端

- [x] 接入文件导入页面与状态查询
- [x] 训练页接入 hint 交互
- [x] 首页展示薄弱项摘要和最近训练摘要
- [x] 页面主链路样式优化
- [x] 空态 / 错误态统一组件化

---

## 第六阶段：P2 预留任务

### 推荐训练

- [x] 设计今日推荐训练包生成策略
- [x] 新增推荐训练接口
- [x] 首页增加推荐训练入口

### 自动组题

- [x] 设计按标签 / 薄弱项 / 历史表现的组题规则
- [x] 扩展 `training_session.total_questions`
- [x] 支持一个 session 内多题

### 回练机制

- [x] 设计回练触发条件
- [x] 设计薄弱项权重更新策略
- [x] 设计回练提醒展示方式

---

## 🧪 测试重点清单

### 安全

- [x] 未携带 token 请求返回 `401`
- [x] 非 owner 访问资源返回 `404` 或 `403`
- [x] 删除知识点不会误删他人数据

### 数据

- [x] `knowledge.user_id` 正确落库
- [x] 标签写入与查询正常
- [x] `training_session` 与 `training_question` 关系正确
- [ ] `missing_points / better_answer_approach / weak_tags` JSON 结构正常

### 体验

- [ ] 登录主链路无死跳转
- [ ] 开始训练到结果页主链路可一次走通
- [ ] 错误提示足够清晰
- [x] LLM 慢响应时加载态明确

---

## 📌 发布前检查

- [ ] `docs/plan` 中 V2 文档已冻结
- [ ] README 当前文档链接可用
- [ ] 前后端环境变量清单补齐
- [ ] 本地 PostgreSQL 初始化步骤清晰
- [ ] 旧 V1 文档已全部归档，不再影响当前设计

---

## ✅ P0 完成定义

只有当以下条件全部满足时，V2 P0 才算完成：

1. 用户可以注册、登录、设置训练方向
2. 用户可以创建、编辑、批量导入自己的知识点
3. 用户可以发起训练并获得新的结构化反馈
4. 用户只能访问自己的知识点与训练记录
5. 前端主链路与后端新契约全部对齐

P1 / P2 任务不得挤占 P0 的开发完成标准。
